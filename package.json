{
  "name": "kabadconnect",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hyperlocal Recycling Marketplace - Doorstep scrap pickup & circular economy platform",
  "scripts": {
    "dev": "npm --prefix kabadconnect-web run dev",
    "build": "npm --prefix kabadconnect-web run build",
    "preview": "npm --prefix kabadconnect-web run preview",
    "test-db": "node scripts/test-db.js",
    "seed-db": "node scripts/seed-db.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "mongoose": "^9.9.5"
  }
}
